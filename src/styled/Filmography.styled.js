import styled from 'styled-components';

const StyledFilmography = styled.section`
    max-width: 1300px;
    margin: auto;
    padding: 3rem 2rem 0;
    color: rgb(var(--color-alfa));

    & h2 {
        margin-bottom: 2rem;
        font-style: italic;
        font-size: 4rem;
    }
`

export default StyledFilmography;